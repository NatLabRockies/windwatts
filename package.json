{
  "private": true,
  "packageManager": "npm@10",
  "scripts": {
    "prepare": "husky",
    "lint": "eslint",
    "format": "prettier"
  },
  "lint-staged": {
    "windwatts-ui/src/**/*.{js,jsx,ts,tsx}": [
      "./check-format.sh"
    ],
    "windwatts-api/**/*.py": [
      "./check-api-format.sh"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  }
}
